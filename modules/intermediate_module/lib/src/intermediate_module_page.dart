import 'package:flutter/material.dart';

class IntermediateModulePage extends StatelessWidget {
  const IntermediateModulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Módulo intermedio',
        ),
      ),
    );
  }
}
