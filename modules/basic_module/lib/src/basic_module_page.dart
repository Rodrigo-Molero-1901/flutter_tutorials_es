import 'package:flutter/material.dart';

class BasicModulePage extends StatelessWidget {
  const BasicModulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Módulo básico',
        ),
      ),
    );
  }
}
