import 'package:flutter/material.dart';

class AdvancedModulePage extends StatelessWidget {
  const AdvancedModulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Módulo avanzado',
        ),
      ),
    );
  }
}
